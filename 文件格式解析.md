## State of War 游戏资源文件结构解析手册

### 1. `sprites.info` - 资源索引文件

**作用**: 作为 `sprites.data` 的目录，提供了每个资源块的名称、在 `.data` 文件中的位置和大小。

**文件结构**:
```
[文件头 (8字节)]
[记录 #1 (可变长)]
[记录 #2 (可变长)]
...
[记录 #N (可变长)]
```

**详细读取方式**:

1.  **读取文件头 (File Header)**:
    *   `0x00` (4字节): **魔数 (Magic Number)**。读取一个32位无符号整数（小端序），应为 `0x01010101`。
    *   `0x04` (4字节): **记录总数 (Total Records)**。读取一个32位无符号整数（小端序），得到文件中记录的总数量 `N`。

2.  **循环读取记录 (Loop `N` times)**:
    *   **ID字符串 (ID String)**:
        *   从当前位置开始，逐字节读取。
        *   对每个字节 `b`，计算 `chr(b - 0x0A)` 得到解密后的ASCII字符。
        *   拼接字符，直到读取到一个空字节 `\x00` 为止。得到的就是资源的完整路径名（如 `units\2c_blue.ps6`）。
    *   **偏移量 (Offset)**:
        *   紧跟在 `\x00` 之后，读取一个32位无符号整数（小端序）。这是该资源在 `sprites.data` 文件中的起始字节偏移。
    *   **长度 (Length)**:
        *   紧跟在偏移量之后，读取一个32位无符号整数（小端序）。这是该资源在 `sprites.data` 文件中占用的字节数。

### 2. `sprites.data` - 资源数据容器

**作用**: 存储所有游戏资源的原始二进制数据。它本身没有独立的结构，是一个纯粹的数据集合，需要依赖 `sprites.info` 来进行解析。

**详细读取方式**:

*   此文件不直接从头读取。
*   当需要加载某个资源时（例如 `units\2c_blue.ps6`），首先在 `sprites.info` 中找到对应的记录，获取其 `offset` 和 `length`。
*   然后打开 `sprites.data` 文件，将文件指针移动到 `offset` 位置，再读取 `length` 个字节。这一块数据就是该资源的完整内容。

---

### 3. `.ps6` - 16位色动画精灵文件

**作用**: 存储游戏单位、特效等的动画序列。每个文件包含一个或多个动画帧，颜色格式为 **RGB565**。

**文件结构**:
```
[帧 #1 数据块 (可变长)]
[帧 #2 数据块 (可变长)]
...
[帧 #N 数据块 (可变长)]
[文件结束标记 (4字节)]
```

**详细读取方式**:

1.  **循环读取帧 (Loop until End of File)**:
    *   **读取帧大小 (Frame Size)**:
        *   从当前位置读取一个32位无符号整数（小端序），得到 `frame_size`。
        *   **如果 `frame_size` 为 0，说明已到达文件末尾，停止读取。**
    *   **记录当前帧块的起始位置 `frame_start_pos`（Frame Size后第一个字节的起始位置）**。

    *   **读取帧元数据 (Frame Metadata)**:
        *   `+0x00` (2字节): **宽度 (Width)**。读取一个16位无符号整数（小端序）。
        *   `+0x02` (2字节): **高度 (Height)**。读取一个16位无符号整数（小端序）。
        *   `+0x04` (2字节): **锚点X (Origin X)**。读取一个16位有符号整数（小端序）。
        *   `+0x06` (2字节): **锚点Y (Origin Y)**。读取一个16位有符号整数（小端序）。
        *   `+0x08` (8字节): **未知数据 (Unknown Data)**。读取4个16位整数，可暂时忽略。

    *   **读取行偏移表 (Line Offset Table)**:
        *   这是一个包含 `height` 个条目的数组，每条目4字节。
        *   循环 `y` 从 `0` 到 `height - 1`:
            *   读取一个32位无符号整数（小端序），得到 `line_offset[y]`。这个值是**以2字节为单位的相对偏移**。

    *   **解码像素数据 (Decode Pixel Data)**:
        *   创建一个大小为 `width * height` 的像素缓冲区。
        *   循环 `y` 从 `0` 到 `height - 1`:
            *   计算当前行的像素数据起始位置：`line_data_pos = frame_start_pos + (line_offset[y] * 2)`。
            *   将文件指针移动到 `line_data_pos`。
            *   **读取RLE指令**:
                1.  `num_commands` (2字节): 读取一个16位无符号整数，表示本行有多少条指令。
                2.  `is_skip_mode_first` (2字节): 读取一个16位整数。如果为`0x0000`，则第一条指令是"绘制"；否则是"跳过"。
                3.  初始化一个布尔变量 `is_skip_mode`。
                4.  循环 `num_commands` 次:
                    *   **如果 `is_skip_mode` 为 `true`**:
                        *   读取一个16位整数 `count`。
                        *   在像素缓冲区中向右移动（填充透明像素）`count` 个位置。
                    *   **如果 `is_skip_mode` 为 `false`**:
                        *   读取一个16位整数 `count`。
                        *   紧接着循环 `count` 次，每次读取一个16位整数，这就是 **RGB565** 颜色值。将其绘制到像素缓冲区。
                    *   切换模式：`is_skip_mode = !is_skip_mode`。

    *   **移动到下一帧**:
        *   将文件指针移动到 `frame_start_pos + frame_size`，准备读取下一帧。

---

### 4. `.po1`, `.po2` - 16位色调色板文件（未解析）

**作用**: 存储用于地图瓦片或UI元素的调色板。颜色格式为 **RGB555**。

**文件结构**:
```
[颜色 #1 (2字节)]
[颜色 #2 (2字节)]
...
[颜色 #N (2字节)]
```

**详细读取方式**:

1.  **确定颜色数量 `N`**:
    *   `N = 文件总大小（字节） / 2`。
    *   例如，512字节的文件包含256个颜色；128字节的文件包含64个颜色。

2.  **循环读取颜色 (Loop `N` times)**:
    *   每次读取一个16位无符号整数（小端序），得到 `color_value`。
    *   **解析RGB555颜色**:
        *   `R = (color_value >> 10) & 0x1F`  (5位，范围0-31)
        *   `G = (color_value >> 5)  & 0x1F`  (5位，范围0-31)
        *   `B = (color_value >> 0)  & 0x1F`  (5位，范围0-31)
    *   为了在现代屏幕上显示，通常需要将5位颜色扩展到8位：`R_8bit = (R * 255) / 31`。

---

### 5. `.sha`, `.msk` - 二值图像文件 (阴影与遮罩)

**作用**: 分别提供精灵的阴影 (`.sha`) 和精确的可见轮廓 (`.msk`)。这两种文件都是二值图，即每个像素只有"开"或"关"两种状态。

**文件结构**:
与 `.ps6` 的宏观结构完全相同，由多个帧数据块组成。
```
[帧 #1 数据块 (可变长)]
...
[帧 #N 数据块 (可变长)]
[文件结束标记 (4字节)]
```

**详细读取方式**:
帧的头部信息（帧大小、图像宽高、锚点、未知数据）和行偏移表的读取方式与 `.ps6` 完全一致。主要区别在于像素数据的解码。

**解码像素数据 (Decode Pixel Data - Binary RLE)**:
`.sha` 和 `.msk` 使用了一种更简化的RLE（行程长度编码）格式，专门用于二值图像。

*   循环 `y` 从 `0` 到 `height - 1`，定位到每一行的RLE数据流。
*   **读取RLE指令**:
    1.  `num_commands` (2字节): 读取一个16位无符号整数，表示本行有多少条指令。
    2.  `is_skip_mode_first` (2字节): 读取一个16位整数。如果为`0x0000`，则第一条指令是"绘制"；否则是"跳过"。
    3.  初始化一个布尔变量 `is_skip_mode`。
    4.  循环 `num_commands` 次:
        *   **读取指令计数值 `count` (2字节)**。
        *   **如果 `is_skip_mode` 为 `true`**:
            *   在像素缓冲区中向右移动（填充透明像素）`count` 个位置。
        *   **如果 `is_skip_mode` 为 `false`**:
            *   在像素缓冲区中绘制 `count` 个**不透明像素**（对于`.sha`是半透明灰色，对于`.msk`是纯色）。
        *   切换模式：`is_skip_mode = !is_skip_mode`。

此格式的特点是，无论是"跳过"还是"绘制"指令，都只包含一个计数值 `count`，而不包含任何颜色数据，从而实现了极高的压缩率。